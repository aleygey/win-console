# dependencies / build output (regenerated by npm run build / dist:win)
node_modules/
out/
dist/

# packaged artifacts
*.tgz
*.tar.gz
*.zip

# logs / local
*.log
.DS_Store
Thumbs.db

# local tooling config (agent/editor)
.claude/

# local runtime data (never committed — may hold the opencode-go key)
**/.opencode/refiner-memory/
**/auth.json
config.local.json
